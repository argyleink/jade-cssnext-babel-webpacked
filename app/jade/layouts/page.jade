include ../mixins/link
include ../mixins/og-meta
include ../mixins/twitter-meta

block config
  - var current = 'main'
  - var pageTitle = 'Jade, Stylus, ES6 & Webpack.'

doctype html
html
  include ../partials/head

  //- block description
  //-   meta(name='description' content=data.meta.description)

  block title
    title= pageTitle

  //- block css
    //- link(rel="stylesheet", href="./css/#{current}.css")

  body
    block content

    script(src='./js/#{current}.js')
