extends layouts/page

append config
  - var current = 'index'

block content
  header(grid='rows' vertically-aligned='center' horizontally-aligned='center')
    .title(grid vertically-aligned='center' horizontally-distributed='around')
      span Jade
      span.spacer |
      span CSSNext
      span.spacer |
      span Babel
    nav(grid vertically-aligned='center' horizontally-distributed='around')
      +link({
        text: 'Home'
      , url:  '/home.html'
      })
      +link({
        text: 'About'
      , url:  '/about.html'
      })
